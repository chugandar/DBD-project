var mysql = require('mysql2/promise');


const config = {
    host: "localhost",
    user: "root",
    password: "",
    database: "npalikams"
}
//var con = mysql.createConnection();
//con.connect();

exports.config = config;